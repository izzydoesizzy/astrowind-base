---
/**
 * ============================================================================
 * TESTIMONIALS INSTRUCTIONS PAGE - BEGINNER-FRIENDLY GUIDE
 * ============================================================================
 * 
 * A comprehensive, step-by-step guide for using testimonials in your Astro pages.
 * Written for people with little to no coding experience.
 * ============================================================================
 */

// @ts-ignore - Astro components are resolved by the Astro compiler
import Layout from '~/layouts/PageLayout.astro';

// @ts-ignore - Astro components are resolved by the Astro compiler
import HeroText from '~/components/widgets/HeroText.astro';

import '~/styles/testimonials.css';
// @ts-ignore - data path resolved by Astro alias
import {
  testimonials,
  getFeaturedTestimonials,
  getVideoTestimonials,
  getLongTestimonials,
  getTestimonialsByCategory,
  getRatedTestimonials,
  getTextTestimonials,
} from '~/data/testimonials';

const metadata = {
  title: 'Testimonials Instructions - Complete Beginner Guide',
};

// Example data for demonstrations
const exampleFeatured = getFeaturedTestimonials().slice(0, 1);
const exampleCoaching = getTestimonialsByCategory('coaching').slice(0, 1);
const exampleWithRating = getRatedTestimonials().slice(0, 1);
---

<Layout metadata={metadata}>
  <HeroText
    tagline="Complete Beginner Guide"
    title="How to Use Testimonials in Your Pages"
    subtitle="Step-by-step instructions written for people with little coding experience. Every step explained clearly!"
  />

  <div class="container mx-auto max-w-4xl px-6 py-16">
    
    <!-- TABLE OF CONTENTS -->
    <section class="mb-16 rounded-2xl border border-slate-200 bg-slate-50 p-8 dark:border-slate-800 dark:bg-slate-900/50">
      <h2 class="mb-6 text-2xl font-bold text-slate-900 dark:text-white">Table of Contents</h2>
      <nav class="space-y-2">
        <a href="#step-1" class="block text-primary hover:underline">Step 1: Import the Files You Need</a>
        <a href="#step-2" class="block text-primary hover:underline">Step 2: Select Which Testimonials to Use</a>
        <a href="#step-3" class="block text-primary hover:underline">Step 3: Choose Your Layout Style</a>
        <a href="#step-4" class="block text-primary hover:underline">Step 4: Add Testimonials to Your Page</a>
        <a href="#all-styles" class="block text-primary hover:underline">All Testimonial Styles with Examples</a>
        <a href="#customization" class="block text-primary hover:underline">Customization Options</a>
      </nav>
    </section>

    <!-- STEP 1: IMPORT FILES -->
    <section id="step-1" class="mb-16 scroll-mt-20">
      <div class="mb-6">
        <span class="inline-block rounded-full bg-primary px-4 py-1 text-sm font-semibold text-white">Step 1</span>
        <h2 class="mt-4 text-3xl font-bold text-slate-900 dark:text-white">Import the Files You Need</h2>
        <p class="mt-2 text-slate-600 dark:text-slate-300">
          Every page that uses testimonials needs to import two things: the data and the styles.
        </p>
      </div>

      <div class="mb-6 rounded-lg border border-amber-200 bg-amber-50 p-4 text-sm text-amber-900 dark:border-amber-500/40 dark:bg-amber-500/10 dark:text-amber-100">
        <p class="font-semibold">✅ Your preset file lives here:</p>
        <p class="mt-2">
          <code class="rounded bg-amber-100 px-1 dark:bg-amber-900/40">src/data/testimonials.preset.json</code> — edit this file to update
          all testimonial text, names, and images in one place.
        </p>
        <p class="mt-2">
          To switch layouts globally, set <code class="rounded bg-amber-100 px-1 dark:bg-amber-900/40">template</code> to one of:
          <code class="rounded bg-amber-100 px-1 dark:bg-amber-900/40">grid</code>,
          <code class="rounded bg-amber-100 px-1 dark:bg-amber-900/40">grid-2</code>,
          <code class="rounded bg-amber-100 px-1 dark:bg-amber-900/40">masonry</code>, or
          <code class="rounded bg-amber-100 px-1 dark:bg-amber-900/40">minimal</code>.
        </p>
      </div>

      <div class="rounded-lg border border-slate-200 bg-white p-6 dark:border-slate-800 dark:bg-slate-900">
        <h3 class="mb-3 font-semibold text-slate-900 dark:text-white">Where to Put This Code</h3>
        <p class="mb-3 text-sm text-slate-600 dark:text-slate-400">
          At the very top of your Astro file, before any HTML. Look for the three dashes <code class="rounded bg-slate-200 px-1 dark:bg-slate-700">---</code> at the top of your file.
        </p>
        <pre class="overflow-x-auto rounded bg-slate-900 p-4 text-sm text-slate-100"><code>{`---
// Import the CSS styles (makes testimonials look good)
import '~/styles/testimonials.css';

// Import the testimonials data
import { testimonials } from '~/data/testimonials';
---`}</code></pre>
        <div class="mt-4 space-y-2 text-sm text-slate-600 dark:text-slate-400">
          <p><strong>Line by line explanation:</strong></p>
          <ul class="ml-4 list-disc space-y-1">
            <li><code class="rounded bg-slate-200 px-1 dark:bg-slate-700">---</code> - This tells Astro "the code section starts here"</li>
            <li><code class="rounded bg-slate-200 px-1 dark:bg-slate-700">import '~/styles/testimonials.css';</code> - This loads the CSS file that makes testimonials look pretty</li>
            <li><code class="rounded bg-slate-200 px-1 dark:bg-slate-700">import { testimonials } from '~/data/testimonials';</code> - This gives you access to all your testimonials</li>
            <li><code class="rounded bg-slate-200 px-1 dark:bg-slate-700">---</code> - This tells Astro "the code section ends here, HTML starts now"</li>
          </ul>
          <p class="mt-3 font-semibold text-slate-900 dark:text-white">💡 Think of it like this:</p>
          <p>Just like you need to open a book before you can read it, you need to "import" (open) these files before you can use them.</p>
        </div>
      </div>
    </section>

    <!-- STEP 2: SELECT TESTIMONIALS -->
    <section id="step-2" class="mb-16 scroll-mt-20">
      <div class="mb-6">
        <span class="inline-block rounded-full bg-primary px-4 py-1 text-sm font-semibold text-white">Step 2</span>
        <h2 class="mt-4 text-3xl font-bold text-slate-900 dark:text-white">Select Which Testimonials to Use</h2>
        <p class="mt-2 text-slate-600 dark:text-slate-300">
          You don't have to use all testimonials. Here are 8 easy ways to pick specific ones.
        </p>
      </div>

      <!-- Method 1: All Testimonials -->
      <div class="mb-8 rounded-lg border border-slate-200 bg-white p-6 dark:border-slate-800 dark:bg-slate-900">
        <h3 class="mb-3 text-xl font-semibold text-slate-900 dark:text-white">Method 1: Use All Testimonials</h3>
        <p class="mb-4 text-slate-600 dark:text-slate-300">Use every testimonial in the list. This will show ALL testimonials you have in your data file.</p>
        <pre class="mb-4 overflow-x-auto rounded bg-slate-900 p-4 text-sm text-slate-100"><code>{`---
import { testimonials } from '~/data/testimonials';

// Use all testimonials
const myTestimonials = testimonials;
---`}</code></pre>
        <div class="rounded-lg border border-slate-200 bg-slate-50 p-4 dark:border-slate-700 dark:bg-slate-800/50">
          <p class="mb-2 text-sm font-semibold text-slate-900 dark:text-white">What this code does:</p>
          <ul class="ml-4 list-disc space-y-1 text-sm text-slate-600 dark:text-slate-300">
            <li><code class="rounded bg-slate-200 px-1 dark:bg-slate-700">import { testimonials }</code> - Gets all testimonials from the data file</li>
            <li><code class="rounded bg-slate-200 px-1 dark:bg-slate-700">const myTestimonials = testimonials;</code> - Saves all testimonials into a variable called "myTestimonials"</li>
            <li>You can then use <code class="rounded bg-slate-200 px-1 dark:bg-slate-700">myTestimonials</code> in your HTML below</li>
          </ul>
        </div>
        <p class="mt-4 text-sm text-slate-600 dark:text-slate-400">
          <strong>When to use:</strong> When you want to show everything you have. Good for a dedicated testimonials page.
        </p>
      </div>

      <!-- Method 2: First N Testimonials -->
      <div class="mb-8 rounded-lg border border-slate-200 bg-white p-6 dark:border-slate-800 dark:bg-slate-900">
        <h3 class="mb-3 text-xl font-semibold text-slate-900 dark:text-white">Method 2: Use First 3, 5, or 10 Testimonials</h3>
        <p class="mb-4 text-slate-600 dark:text-slate-300">Show only the first few testimonials from your list. Perfect when you don't want to show everything.</p>
        <pre class="mb-4 overflow-x-auto rounded bg-slate-900 p-4 text-sm text-slate-100"><code>{`---
import { testimonials } from '~/data/testimonials';

// First 3 testimonials
const myTestimonials = testimonials.slice(0, 3);

// First 5 testimonials (uncomment to use this instead)
// const myTestimonials = testimonials.slice(0, 5);

// First 10 testimonials (uncomment to use this instead)
// const myTestimonials = testimonials.slice(0, 10);
---`}</code></pre>
        <div class="rounded-lg border border-slate-200 bg-slate-50 p-4 dark:border-slate-700 dark:bg-slate-800/50">
          <p class="mb-2 text-sm font-semibold text-slate-900 dark:text-white">How it works:</p>
          <ul class="ml-4 list-disc space-y-1 text-sm text-slate-600 dark:text-slate-300">
            <li><code class="rounded bg-slate-200 px-1 dark:bg-slate-700">.slice(0, 3)</code> means "start at position 0, take 3 items"</li>
            <li><code class="rounded bg-slate-200 px-1 dark:bg-slate-700">.slice(0, 5)</code> means "start at position 0, take 5 items"</li>
            <li>Change the number (3, 5, or 10) to show however many you want</li>
            <li>Lines starting with <code class="rounded bg-slate-200 px-1 dark:bg-slate-700">//</code> are comments (ignored by the computer)</li>
          </ul>
          <p class="mt-2 text-sm text-slate-600 dark:text-slate-300">
            <strong>💡 Think of it like this:</strong> It's like saying "give me the first 3 items from the list" - just like taking the first 3 apples from a basket.
          </p>
        </div>
        <p class="mt-4 text-sm text-slate-600 dark:text-slate-400">
          <strong>When to use:</strong> When you want a small selection, like for a homepage. Shows just enough to build trust without overwhelming visitors.
        </p>
      </div>

      <!-- Method 3: By Category -->
      <div class="mb-8 rounded-lg border border-slate-200 bg-white p-6 dark:border-slate-800 dark:bg-slate-900">
        <h3 class="mb-3 text-xl font-semibold text-slate-900 dark:text-white">Method 3: Use Testimonials by Category</h3>
        <p class="mb-4 text-slate-600 dark:text-slate-300">Show only testimonials from a specific category (like "coaching" or "product").</p>
        <pre class="mb-4 overflow-x-auto rounded bg-slate-900 p-4 text-sm text-slate-100"><code>{`---
import { getTestimonialsByCategory } from '~/data/testimonials';

// Get only coaching testimonials
const myTestimonials = getTestimonialsByCategory('coaching');

// Other categories you can use:
// - 'product'
// - 'growth'
// - 'sales'
// - 'agency'
// - 'leadership'
// - 'business'
// - 'design'
// - 'general'
---`}</code></pre>
        <div class="rounded-lg border border-slate-200 bg-slate-50 p-4 dark:border-slate-700 dark:bg-slate-800/50">
          <p class="mb-2 text-sm font-semibold text-slate-900 dark:text-white">How it works:</p>
          <ul class="ml-4 list-disc space-y-1 text-sm text-slate-600 dark:text-slate-300">
            <li><code class="rounded bg-slate-200 px-1 dark:bg-slate-700">getTestimonialsByCategory('coaching')</code> - Finds all testimonials with category "coaching"</li>
            <li>Change <code class="rounded bg-slate-200 px-1 dark:bg-slate-700">'coaching'</code> to any category name (keep the quotes!)</li>
            <li>Categories are like tags - each testimonial can have one category</li>
          </ul>
          <p class="mt-2 text-sm text-slate-600 dark:text-slate-300">
            <strong>💡 Think of it like this:</strong> It's like filtering emails by folder - you're only showing testimonials from one specific folder.
          </p>
        </div>
        <p class="mt-4 text-sm text-slate-600 dark:text-slate-400">
          <strong>When to use:</strong> When you want testimonials related to a specific topic or service. Perfect for service-specific pages.
        </p>
      </div>

      <!-- Method 4: Featured Only -->
      <div class="mb-8 rounded-lg border border-slate-200 bg-white p-6 dark:border-slate-800 dark:bg-slate-900">
        <h3 class="mb-3 text-xl font-semibold text-slate-900 dark:text-white">Method 4: Use Featured Testimonials Only</h3>
        <p class="mb-4 text-slate-600 dark:text-slate-300">Show only the testimonials marked as "featured" (your best ones).</p>
        <pre class="mb-4 overflow-x-auto rounded bg-slate-900 p-4 text-sm text-slate-100"><code>{`---
import { getFeaturedTestimonials } from '~/data/testimonials';

// Get only featured testimonials
const myTestimonials = getFeaturedTestimonials();
---`}</code></pre>
        <div class="rounded-lg border border-slate-200 bg-slate-50 p-4 dark:border-slate-700 dark:bg-slate-800/50">
          <p class="mb-2 text-sm font-semibold text-slate-900 dark:text-white">How it works:</p>
          <ul class="ml-4 list-disc space-y-1 text-sm text-slate-600 dark:text-slate-300">
            <li><code class="rounded bg-slate-200 px-1 dark:bg-slate-700">getFeaturedTestimonials()</code> - Finds all testimonials where "featured" is set to true</li>
            <li>To mark a testimonial as featured, edit <code class="rounded bg-slate-200 px-1 dark:bg-slate-700">src/data/testimonials.ts</code> and set <code class="rounded bg-slate-200 px-1 dark:bg-slate-700">featured: true</code></li>
          </ul>
        </div>
        <p class="mt-4 text-sm text-slate-600 dark:text-slate-400">
          <strong>When to use:</strong> When you want to highlight your best testimonials. Perfect for homepage hero sections.
        </p>
      </div>

      <!-- Method 5: With Star Ratings -->
      <div class="mb-8 rounded-lg border border-slate-200 bg-white p-6 dark:border-slate-800 dark:bg-slate-900">
        <h3 class="mb-3 text-xl font-semibold text-slate-900 dark:text-white">Method 5: Use Only Testimonials with Star Ratings</h3>
        <p class="mb-4 text-slate-600 dark:text-slate-300">Show only testimonials that have star ratings (1-5 stars).</p>
        <pre class="mb-4 overflow-x-auto rounded bg-slate-900 p-4 text-sm text-slate-100"><code>{`---
import { getRatedTestimonials } from '~/data/testimonials';

// Get only testimonials with ratings
const myTestimonials = getRatedTestimonials();

// Or get only 5-star testimonials
// const myTestimonials = testimonials.filter(t => t.rating === 5);
---`}</code></pre>
        <div class="rounded-lg border border-slate-200 bg-slate-50 p-4 dark:border-slate-700 dark:bg-slate-800/50">
          <p class="mb-2 text-sm font-semibold text-slate-900 dark:text-white">How it works:</p>
          <ul class="ml-4 list-disc space-y-1 text-sm text-slate-600 dark:text-slate-300">
            <li><code class="rounded bg-slate-200 px-1 dark:bg-slate-700">getRatedTestimonials()</code> - Finds all testimonials that have a rating (1-5 stars)</li>
            <li>The second option (commented out) shows only 5-star testimonials</li>
            <li>Ratings are numbers: 1, 2, 3, 4, or 5</li>
          </ul>
        </div>
        <p class="mt-4 text-sm text-slate-600 dark:text-slate-400">
          <strong>When to use:</strong> When you want to show social proof with star ratings. Great for building trust.
        </p>
      </div>

      <!-- Method 6: Video Testimonials -->
      <div class="mb-8 rounded-lg border border-slate-200 bg-white p-6 dark:border-slate-800 dark:bg-slate-900">
        <h3 class="mb-3 text-xl font-semibold text-slate-900 dark:text-white">Method 6: Use Only Video Testimonials</h3>
        <p class="mb-4 text-slate-600 dark:text-slate-300">Show only testimonials that have videos.</p>
        <pre class="mb-4 overflow-x-auto rounded bg-slate-900 p-4 text-sm text-slate-100"><code>{`---
import { getVideoTestimonials } from '~/data/testimonials';

// Get only video testimonials
const myTestimonials = getVideoTestimonials();
---`}</code></pre>
        <p class="text-sm text-slate-600 dark:text-slate-400">
          <strong>When to use:</strong> When you want to showcase video testimonials. Videos are more engaging than text.
        </p>
      </div>

      <!-- Method 7: Text Only -->
      <div class="mb-8 rounded-lg border border-slate-200 bg-white p-6 dark:border-slate-800 dark:bg-slate-900">
        <h3 class="mb-3 text-xl font-semibold text-slate-900 dark:text-white">Method 7: Use Only Text Testimonials (No Videos)</h3>
        <p class="mb-4 text-slate-600 dark:text-slate-300">Show only testimonials without videos (text-only).</p>
        <pre class="mb-4 overflow-x-auto rounded bg-slate-900 p-4 text-sm text-slate-100"><code>{`---
import { getTextTestimonials } from '~/data/testimonials';

// Get only text testimonials (no videos)
const myTestimonials = getTextTestimonials();
---`}</code></pre>
        <p class="text-sm text-slate-600 dark:text-slate-400">
          <strong>When to use:</strong> When you want simple text testimonials without videos. Faster loading, simpler design.
        </p>
      </div>

      <!-- Method 8: Specific IDs -->
      <div class="mb-8 rounded-lg border border-slate-200 bg-white p-6 dark:border-slate-800 dark:bg-slate-900">
        <h3 class="mb-3 text-xl font-semibold text-slate-900 dark:text-white">Method 8: Pick Specific Testimonials by ID</h3>
        <p class="mb-4 text-slate-600 dark:text-slate-300">Choose exactly which testimonials you want by their ID numbers.</p>
        <pre class="mb-4 overflow-x-auto rounded bg-slate-900 p-4 text-sm text-slate-100"><code>{`---
import { testimonials } from '~/data/testimonials';

// Pick specific testimonials by their ID
const myTestimonials = testimonials.filter(t => 
  ['testimonial-1', 'testimonial-3', 'testimonial-5'].includes(t.id)
);
---`}</code></pre>
        <div class="rounded-lg border border-slate-200 bg-slate-50 p-4 dark:border-slate-700 dark:bg-slate-800/50">
          <p class="mb-2 text-sm font-semibold text-slate-900 dark:text-white">How it works:</p>
          <ul class="ml-4 list-disc space-y-1 text-sm text-slate-600 dark:text-slate-300">
            <li>Each testimonial has a unique ID like <code class="rounded bg-slate-200 px-1 dark:bg-slate-700">'testimonial-1'</code></li>
            <li>List the IDs you want inside the square brackets <code class="rounded bg-slate-200 px-1 dark:bg-slate-700">[]</code></li>
            <li>Change <code class="rounded bg-slate-200 px-1 dark:bg-slate-700">'testimonial-1', 'testimonial-3', 'testimonial-5'</code> to the IDs you want</li>
            <li>To find IDs, open <code class="rounded bg-slate-200 px-1 dark:bg-slate-700">src/data/testimonials.ts</code> and look for <code class="rounded bg-slate-200 px-1 dark:bg-slate-700">id: 'testimonial-X'</code></li>
          </ul>
        </div>
        <p class="mt-4 text-sm text-slate-600 dark:text-slate-400">
          <strong>When to use:</strong> When you want complete control over exactly which testimonials appear. Perfect for curated selections.
        </p>
      </div>

      <!-- Combining Methods -->
      <div class="mb-8 rounded-lg border-2 border-primary bg-primary/5 p-6 dark:bg-primary/10">
        <h3 class="mb-3 text-xl font-semibold text-slate-900 dark:text-white">💡 Pro Tip: Combine Methods</h3>
        <p class="mb-4 text-slate-600 dark:text-slate-300">You can combine methods to get exactly what you want!</p>
        <pre class="mb-4 overflow-x-auto rounded bg-slate-900 p-4 text-sm text-slate-100"><code>{`---
import { testimonials } from '~/data/testimonials';

// Example: Get coaching testimonials with 5-star ratings, first 3
const myTestimonials = testimonials
  .filter(t => t.category === 'coaching' && t.rating === 5)
  .slice(0, 3);
---`}</code></pre>
        <p class="text-sm text-slate-600 dark:text-slate-400">
          This example finds coaching testimonials that are 5-star rated, then takes only the first 3.
        </p>
      </div>
    </section>

    <!-- STEP 3: CHOOSE LAYOUT -->
    <section id="step-3" class="mb-16 scroll-mt-20">
      <div class="mb-6">
        <span class="inline-block rounded-full bg-primary px-4 py-1 text-sm font-semibold text-white">Step 3</span>
        <h2 class="mt-4 text-3xl font-bold text-slate-900 dark:text-white">Choose Your Layout Style</h2>
        <p class="mt-2 text-slate-600 dark:text-slate-300">
          Pick how you want your testimonials to look. Each style uses different CSS classes.
        </p>
      </div>

      <!-- Style 1: Standard Grid -->
      <div class="mb-8 rounded-lg border border-slate-200 bg-white p-6 dark:border-slate-800 dark:bg-slate-900">
        <h3 class="mb-3 text-xl font-semibold text-slate-900 dark:text-white">Style 1: Standard Grid (3 Columns)</h3>
        <p class="mb-4 text-slate-600 dark:text-slate-300">Cards arranged in a grid. Shows 1 column on mobile, 2 on tablet, 3 on desktop.</p>
        <p class="mb-2 text-sm font-semibold text-slate-700 dark:text-slate-300">Use this class: <code class="rounded bg-slate-200 px-2 py-1 dark:bg-slate-700">testimonial-grid</code></p>
        <div class="rounded-lg border border-slate-200 bg-slate-50 p-4 dark:border-slate-700 dark:bg-slate-800/50">
          <p class="mb-2 text-sm font-semibold text-slate-900 dark:text-white">What it looks like:</p>
          <p class="text-sm text-slate-600 dark:text-slate-300">
            Testimonials appear in a neat grid. On phones: 1 per row. On tablets: 2 per row. On computers: 3 per row. Each testimonial is in a white card with a shadow, showing the person's photo, their quote, name, and job title.
          </p>
        </div>
        <p class="mt-4 text-sm text-slate-600 dark:text-slate-400">
          <strong>Best for:</strong> General testimonial sections, product pages, service pages.
        </p>
      </div>

      <!-- Style 2: Two Column -->
      <div class="mb-8 rounded-lg border border-slate-200 bg-white p-6 dark:border-slate-800 dark:bg-slate-900">
        <h3 class="mb-3 text-xl font-semibold text-slate-900 dark:text-white">Style 2: Two Column Grid</h3>
        <p class="mb-4 text-slate-600 dark:text-slate-300">Cards in 2 columns. Shows 1 column on mobile, 2 on larger screens.</p>
        <p class="mb-2 text-sm font-semibold text-slate-700 dark:text-slate-300">Use this class: <code class="rounded bg-slate-200 px-2 py-1 dark:bg-slate-700">testimonial-grid-2</code></p>
        <div class="rounded-lg border border-slate-200 bg-slate-50 p-4 dark:border-slate-700 dark:bg-slate-800/50">
          <p class="mb-2 text-sm font-semibold text-slate-900 dark:text-white">What it looks like:</p>
          <p class="text-sm text-slate-600 dark:text-slate-300">
            Similar to Style 1, but only 2 columns on larger screens. Cards are wider, giving more space for longer testimonials. Perfect when you want testimonials to be more prominent.
          </p>
        </div>
        <p class="mt-4 text-sm text-slate-600 dark:text-slate-400">
          <strong>Best for:</strong> When you want larger cards with more space.
        </p>
      </div>

      <!-- Style 3: Masonry -->
      <div class="mb-8 rounded-lg border border-slate-200 bg-white p-6 dark:border-slate-800 dark:bg-slate-900">
        <h3 class="mb-3 text-xl font-semibold text-slate-900 dark:text-white">Style 3: Masonry (Staggered Columns)</h3>
        <p class="mb-4 text-slate-600 dark:text-slate-300">Cards of different heights arranged in columns, like Pinterest.</p>
        <p class="mb-2 text-sm font-semibold text-slate-700 dark:text-slate-300">Use these classes: <code class="rounded bg-slate-200 px-2 py-1 dark:bg-slate-700">testimonial-masonry</code> and <code class="rounded bg-slate-200 px-2 py-1 dark:bg-slate-700">testimonial-masonry-item</code></p>
        <div class="rounded-lg border border-slate-200 bg-slate-50 p-4 dark:border-slate-700 dark:bg-slate-800/50">
          <p class="mb-2 text-sm font-semibold text-slate-900 dark:text-white">What it looks like:</p>
          <p class="text-sm text-slate-600 dark:text-slate-300">
            Testimonials are arranged in columns like a brick wall. Short testimonials sit next to tall ones, creating a dynamic, Pinterest-style layout. Great for showing many testimonials of different lengths.
          </p>
        </div>
        <p class="mt-4 text-sm text-slate-600 dark:text-slate-400">
          <strong>Best for:</strong> When testimonials have very different lengths. Creates visual interest.
        </p>
      </div>

      <!-- Style 4: Featured Card -->
      <div class="mb-8 rounded-lg border border-slate-200 bg-white p-6 dark:border-slate-800 dark:bg-slate-900">
        <h3 class="mb-3 text-xl font-semibold text-slate-900 dark:text-white">Style 4: Featured Card</h3>
        <p class="mb-4 text-slate-600 dark:text-slate-300">A highlighted card with gradient background to make one testimonial stand out.</p>
        <p class="mb-2 text-sm font-semibold text-slate-700 dark:text-slate-300">Use this class: <code class="rounded bg-slate-200 px-2 py-1 dark:bg-slate-700">testimonial-card-featured</code></p>
        <div class="rounded-lg border border-slate-200 bg-slate-50 p-4 dark:border-slate-700 dark:bg-slate-800/50">
          <p class="mb-2 text-sm font-semibold text-slate-900 dark:text-white">What it looks like:</p>
          <p class="text-sm text-slate-600 dark:text-slate-300">
            One testimonial appears in a special highlighted box with a colored gradient background (usually blue). The card is larger, has more padding, and stands out from regular testimonials. Perfect for your best testimonial.
          </p>
        </div>
        <p class="mt-4 text-sm text-slate-600 dark:text-slate-400">
          <strong>Best for:</strong> Highlighting your best testimonial at the top of a section.
        </p>
      </div>

      <!-- Style 5: Minimal Card -->
      <div class="mb-8 rounded-lg border border-slate-200 bg-white p-6 dark:border-slate-800 dark:bg-slate-900">
        <h3 class="mb-3 text-xl font-semibold text-slate-900 dark:text-white">Style 5: Minimal Card</h3>
        <p class="mb-4 text-slate-600 dark:text-slate-300">Simple card with just a border, no shadow. Clean and minimal look.</p>
        <p class="mb-2 text-sm font-semibold text-slate-700 dark:text-slate-300">Use this class: <code class="rounded bg-slate-200 px-2 py-1 dark:bg-slate-700">testimonial-card-minimal</code></p>
        <div class="rounded-lg border border-slate-200 bg-slate-50 p-4 dark:border-slate-700 dark:bg-slate-800/50">
          <p class="mb-2 text-sm font-semibold text-slate-900 dark:text-white">What it looks like:</p>
          <p class="text-sm text-slate-600 dark:text-slate-300">
            Clean white cards with a thin border around them. No shadow, no fancy effects - just simple and elegant. The border changes color slightly when you hover over it.
          </p>
        </div>
        <p class="mt-4 text-sm text-slate-600 dark:text-slate-400">
          <strong>Best for:</strong> Clean, modern designs without heavy shadows. Minimalist aesthetic.
        </p>
      </div>

      <!-- Style 6: Standard Card -->
      <div class="mb-8 rounded-lg border border-slate-200 bg-white p-6 dark:border-slate-800 dark:bg-slate-900">
        <h3 class="mb-3 text-xl font-semibold text-slate-900 dark:text-white">Style 6: Standard Card</h3>
        <p class="mb-4 text-slate-600 dark:text-slate-300">Classic card with shadow. This is the default style.</p>
        <p class="mb-2 text-sm font-semibold text-slate-700 dark:text-slate-300">Use this class: <code class="rounded bg-slate-200 px-2 py-1 dark:bg-slate-700">testimonial-card</code></p>
        <div class="rounded-lg border border-slate-200 bg-slate-50 p-4 dark:border-slate-700 dark:bg-slate-800/50">
          <p class="mb-2 text-sm font-semibold text-slate-900 dark:text-white">What it looks like:</p>
          <p class="text-sm text-slate-600 dark:text-slate-300">
            White cards with a subtle shadow underneath, making them appear to float above the page. Classic, professional look that works everywhere.
          </p>
        </div>
        <p class="mt-4 text-sm text-slate-600 dark:text-slate-400">
          <strong>Best for:</strong> Most general use cases. Works well everywhere.
        </p>
      </div>
    </section>

    <!-- STEP 4: ADD TO PAGE -->
    <section id="step-4" class="mb-16 scroll-mt-20">
      <div class="mb-6">
        <span class="inline-block rounded-full bg-primary px-4 py-1 text-sm font-semibold text-white">Step 4</span>
        <h2 class="mt-4 text-3xl font-bold text-slate-900 dark:text-white">Add Testimonials to Your Page</h2>
        <p class="mt-2 text-slate-600 dark:text-slate-300">
          Now put it all together! Copy one of these examples and modify it for your needs.
        </p>
      </div>

      <!-- Example 1: Simple Grid -->
      <div class="mb-8 rounded-lg border border-slate-200 bg-white p-6 dark:border-slate-800 dark:bg-slate-900">
        <h3 class="mb-4 text-xl font-semibold text-slate-900 dark:text-white">Example 1: Simple Grid with All Testimonials</h3>
        <p class="mb-4 text-slate-600 dark:text-slate-300">Shows first 6 testimonials in a 3-column grid with profile pictures.</p>
        <pre class="mb-4 overflow-x-auto rounded bg-slate-900 p-4 text-sm text-slate-100"><code>{`---
import '~/styles/testimonials.css';
import { testimonials } from '~/data/testimonials';

const myTestimonials = testimonials.slice(0, 6);
---

<div class="testimonial-grid">
  {myTestimonials.map((item) => (
    <article class="testimonial-card">
      <p class="testimonial-content text-slate-800 dark:text-slate-200">
        {item.testimonial}
      </p>
      <div class="mt-4 flex items-center gap-3">
        <div class="testimonial-avatar testimonial-avatar-show testimonial-avatar-md rounded-full overflow-hidden">
          <img src={item.image.src} alt={item.image.alt} class="h-full w-full object-cover" />
        </div>
        <div class="testimonial-author">
          <p class="font-semibold text-slate-900 dark:text-white">{item.name}</p>
          <p class="text-sm text-slate-500 dark:text-slate-400">{item.job}</p>
        </div>
      </div>
    </article>
  ))}
</div>`}</code></pre>
        <div class="rounded-lg border border-slate-200 bg-slate-50 p-4 dark:border-slate-700 dark:bg-slate-800/50">
          <p class="mb-2 text-sm font-semibold text-slate-900 dark:text-white">What this creates:</p>
          <p class="text-sm text-slate-600 dark:text-slate-300">
            A grid of 6 testimonial cards. Each card shows: the testimonial text, a circular profile picture, the person's name in bold, and their job title below the name. Cards are arranged in 3 columns on desktop, 2 on tablet, 1 on mobile.
          </p>
        </div>
      </div>

      <!-- Example 2: With Star Ratings -->
      <div class="mb-8 rounded-lg border border-slate-200 bg-white p-6 dark:border-slate-800 dark:bg-slate-900">
        <h3 class="mb-4 text-xl font-semibold text-slate-900 dark:text-white">Example 2: Grid with Star Ratings</h3>
        <p class="mb-4 text-slate-600 dark:text-slate-300">Shows only testimonials with star ratings, displaying the stars above each quote.</p>
        <pre class="mb-4 overflow-x-auto rounded bg-slate-900 p-4 text-sm text-slate-100"><code>{`---
import '~/styles/testimonials.css';
import { getRatedTestimonials } from '~/data/testimonials';

const myTestimonials = getRatedTestimonials().slice(0, 6);
---

<div class="testimonial-grid">
  {myTestimonials.map((item) => (
    <article class="testimonial-card">
      {item.rating && (
        <div class="testimonial-rating testimonial-rating-show mb-3 flex items-center gap-1" data-rating={item.rating}>
          {[0, 1, 2, 3, 4].map((i) => (
            <svg
              class={\`testimonial-star \${i < item.rating ? 'text-yellow-400 fill-current' : 'testimonial-star-empty'}\`}
              viewBox="0 0 24 24"
            >
              <path d="M11.049 2.927c.3-.921 1.603-.921 1.902 0l1.519 4.674a1 1 0 00.95.69h4.915c.969 0 1.371 1.24.588 1.81l-3.976 2.888a1 1 0 00-.363 1.118l1.518 4.674c.3.922-.755 1.688-1.538 1.118l-3.976-2.888a1 1 0 00-1.176 0l-3.976 2.888c-.783.57-1.838-.197-1.538-1.118l1.518-4.674a1 1 0 00-.363-1.118l-3.976-2.888c-.784-.57-.38-1.81.588-1.81h4.914a1 1 0 00.951-.69l1.519-4.674z" />
            </svg>
          ))}
        </div>
      )}
      <p class="testimonial-content text-slate-800 dark:text-slate-200">
        {item.testimonial}
      </p>
      <div class="mt-4 flex items-center gap-3">
        <div class="testimonial-avatar testimonial-avatar-show testimonial-avatar-md rounded-full overflow-hidden">
          <img src={item.image.src} alt={item.image.alt} class="h-full w-full object-cover" />
        </div>
        <div class="testimonial-author">
          <p class="font-semibold text-slate-900 dark:text-white">{item.name}</p>
          <p class="text-sm text-slate-500 dark:text-slate-400">{item.job}</p>
        </div>
      </div>
    </article>
  ))}
</div>`}</code></pre>
        <div class="rounded-lg border border-slate-200 bg-slate-50 p-4 dark:border-slate-700 dark:bg-slate-800/50">
          <p class="mb-2 text-sm font-semibold text-slate-900 dark:text-white">What this creates:</p>
          <p class="text-sm text-slate-600 dark:text-slate-300">
            Same as Example 1, but with yellow star ratings displayed at the top of each card. If a testimonial has a 5-star rating, you'll see 5 filled yellow stars. If it has 4 stars, you'll see 4 filled stars and 1 empty star.
          </p>
        </div>
      </div>

      <!-- Example 3: Featured First -->
      <div class="mb-8 rounded-lg border border-slate-200 bg-white p-6 dark:border-slate-800 dark:bg-slate-900">
        <h3 class="mb-4 text-xl font-semibold text-slate-900 dark:text-white">Example 3: Featured Testimonial First, Then Grid</h3>
        <p class="mb-4 text-slate-600 dark:text-slate-300">Shows one featured testimonial in a highlighted box, then a grid of other testimonials below.</p>
        <pre class="mb-4 overflow-x-auto rounded bg-slate-900 p-4 text-sm text-slate-100"><code>{`---
import '~/styles/testimonials.css';
import { getFeaturedTestimonials, testimonials } from '~/data/testimonials';

const featured = getFeaturedTestimonials().slice(0, 1);
const others = testimonials.slice(0, 6);
---

{featured.length > 0 && (
  <div class="mb-8 rounded-2xl border border-primary/20 bg-gradient-to-br from-primary/10 to-primary/5 p-8">
    {featured.map((item) => (
      <div class="grid gap-6 md:grid-cols-[auto,1fr] md:items-center">
        <div class="testimonial-avatar testimonial-avatar-show testimonial-avatar-lg rounded-full overflow-hidden">
          <img src={item.image.src} alt={item.image.alt} />
        </div>
        <div>
          <p class="testimonial-content text-lg">{item.testimonial}</p>
          <div class="mt-4 testimonial-author">
            <p class="font-semibold">{item.name}</p>
            <p class="text-sm text-slate-500">{item.job}</p>
          </div>
        </div>
      </div>
    ))}
  </div>
)}

<div class="testimonial-grid">
  {others.map((item) => (
    <article class="testimonial-card">
      <p class="testimonial-content">{item.testimonial}</p>
      <div class="mt-4 flex items-center gap-3">
        <div class="testimonial-avatar testimonial-avatar-show testimonial-avatar-md rounded-full overflow-hidden">
          <img src={item.image.src} alt={item.image.alt} />
        </div>
        <div class="testimonial-author">
          <p class="font-semibold">{item.name}</p>
          <p class="text-sm text-slate-500">{item.job}</p>
        </div>
      </div>
    </article>
  ))}
</div>`}</code></pre>
        <div class="rounded-lg border border-slate-200 bg-slate-50 p-4 dark:border-slate-700 dark:bg-slate-800/50">
          <p class="mb-2 text-sm font-semibold text-slate-900 dark:text-white">What this creates:</p>
          <p class="text-sm text-slate-600 dark:text-slate-300">
            At the top: One large featured testimonial in a special highlighted box with a blue gradient background. The person's photo is on the left (larger than normal), and the testimonial text is on the right. Below that: A regular 3-column grid of 6 other testimonials in standard white cards.
          </p>
        </div>
      </div>

      <!-- Example 4: By Category -->
      <div class="mb-8 rounded-lg border border-slate-200 bg-white p-6 dark:border-slate-800 dark:bg-slate-900">
        <h3 class="mb-4 text-xl font-semibold text-slate-900 dark:text-white">Example 4: Show Only Coaching Testimonials</h3>
        <p class="mb-4 text-slate-600 dark:text-slate-300">Shows only testimonials from the "coaching" category using minimal card style.</p>
        <pre class="mb-4 overflow-x-auto rounded bg-slate-900 p-4 text-sm text-slate-100"><code>{`---
import '~/styles/testimonials.css';
import { getTestimonialsByCategory } from '~/data/testimonials';

const myTestimonials = getTestimonialsByCategory('coaching');
---

<div class="testimonial-grid">
  {myTestimonials.map((item) => (
    <article class="testimonial-card testimonial-card-minimal">
      <p class="testimonial-content">{item.testimonial}</p>
      <div class="mt-4 flex items-center gap-3">
        <div class="testimonial-avatar testimonial-avatar-show testimonial-avatar-md rounded-full overflow-hidden">
          <img src={item.image.src} alt={item.image.alt} />
        </div>
        <div class="testimonial-author">
          <p class="font-semibold">{item.name}</p>
          <p class="text-sm text-slate-500">{item.job}</p>
        </div>
      </div>
    </article>
  ))}
</div>`}</code></pre>
        <div class="rounded-lg border border-slate-200 bg-slate-50 p-4 dark:border-slate-700 dark:bg-slate-800/50">
          <p class="mb-2 text-sm font-semibold text-slate-900 dark:text-white">What this creates:</p>
          <p class="text-sm text-slate-600 dark:text-slate-300">
            A grid showing only testimonials that are categorized as "coaching". Cards use the minimal style (thin border, no shadow). Perfect for a coaching services page.
          </p>
        </div>
      </div>

      <!-- Example 5: Masonry -->
      <div class="mb-8 rounded-lg border border-slate-200 bg-white p-6 dark:border-slate-800 dark:bg-slate-900">
        <h3 class="mb-4 text-xl font-semibold text-slate-900 dark:text-white">Example 5: Masonry Layout (No Avatars)</h3>
        <p class="mb-4 text-slate-600 dark:text-slate-300">Shows testimonials in a staggered masonry layout without profile pictures.</p>
        <pre class="mb-4 overflow-x-auto rounded bg-slate-900 p-4 text-sm text-slate-100"><code>{`---
import '~/styles/testimonials.css';
import { testimonials } from '~/data/testimonials';

const myTestimonials = testimonials.slice(0, 9);
---

<div class="testimonial-masonry">
  {myTestimonials.map((item) => (
    <article class="testimonial-masonry-item testimonial-card">
      <p class="testimonial-content">{item.testimonial}</p>
      <div class="mt-4 testimonial-author">
        <div class="testimonial-avatar testimonial-avatar-hide"></div>
        <p class="font-semibold">{item.name}</p>
        <p class="text-sm text-slate-500">{item.job}</p>
      </div>
    </article>
  ))}
</div>`}</code></pre>
        <div class="rounded-lg border border-slate-200 bg-slate-50 p-4 dark:border-slate-700 dark:bg-slate-800/50">
          <p class="mb-2 text-sm font-semibold text-slate-900 dark:text-white">What this creates:</p>
          <p class="text-sm text-slate-600 dark:text-slate-300">
            Testimonials arranged in a staggered, Pinterest-style layout. No profile pictures - just the testimonial text, name, and job title. Cards are different heights based on how long each testimonial is, creating a dynamic, interesting visual.
          </p>
        </div>
      </div>
    </section>

    <!-- ALL STYLES FROM HUB -->
    <section id="all-styles" class="mb-16 scroll-mt-20">
      <div class="mb-6">
        <h2 class="text-3xl font-bold text-slate-900 dark:text-white">All Testimonial Styles from the Hub Page</h2>
        <p class="mt-2 text-slate-600 dark:text-slate-300">
          Here are complete examples of every style shown on the Testimonials Hub page, with exact code you can copy.
        </p>
      </div>

      <!-- Style 1: Standard Grid (from Hub) -->
      <div class="mb-12 rounded-lg border-2 border-primary bg-primary/5 p-6 dark:bg-primary/10">
        <h3 class="mb-4 text-2xl font-semibold text-slate-900 dark:text-white">Style 1: Standard Grid (From Hub Page)</h3>
        <p class="mb-4 text-slate-600 dark:text-slate-300">
          <strong>What it looks like:</strong> A clean 3-column grid of testimonial cards. Each card has a white background with shadow, shows the testimonial text, and below it: a circular profile picture on the left, with the person's name (bold) and job title next to it. If the testimonial has a rating, you'll see yellow stars at the top.
        </p>
        <pre class="mb-4 overflow-x-auto rounded bg-slate-900 p-4 text-sm text-slate-100"><code>{`---
import '~/styles/testimonials.css';
import { testimonials } from '~/data/testimonials';

const myTestimonials = testimonials.slice(0, 6);
---

<div class="testimonial-grid">
  {myTestimonials.map((item) => (
    <article class="testimonial-card">
      {item.rating && (
        <div class="testimonial-rating testimonial-rating-show mb-3 flex items-center gap-1" data-rating={item.rating}>
          {[0, 1, 2, 3, 4].map((i) => (
            <svg
              class={\`testimonial-star \${i < item.rating ? 'text-yellow-400 fill-current' : 'testimonial-star-empty'}\`}
              viewBox="0 0 24 24"
              aria-hidden="true"
            >
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                stroke-width="2"
                d="M11.049 2.927c.3-.921 1.603-.921 1.902 0l1.519 4.674a1 1 0 00.95.69h4.915c.969 0 1.371 1.24.588 1.81l-3.976 2.888a1 1 0 00-.363 1.118l1.518 4.674c.3.922-.755 1.688-1.538 1.118l-3.976-2.888a1 1 0 00-1.176 0l-3.976 2.888c-.783.57-1.838-.197-1.538-1.118l1.518-4.674a1 1 0 00-.363-1.118l-3.976-2.888c-.784-.57-.38-1.81.588-1.81h4.914a1 1 0 00.951-.69l1.519-4.674z"
              />
            </svg>
          ))}
        </div>
      )}
      <p class="testimonial-content text-slate-800 dark:text-slate-200">{item.testimonial}</p>
      <div class="mt-4 flex items-center gap-3">
        <div class="testimonial-avatar testimonial-avatar-show testimonial-avatar-md rounded-full overflow-hidden">
          <img src={item.image.src} alt={item.image.alt} class="h-full w-full object-cover" />
        </div>
        <div class="testimonial-author">
          <p class="font-semibold text-slate-900 dark:text-white">{item.name}</p>
          <p class="text-sm text-slate-500 dark:text-slate-400">{item.job}</p>
        </div>
      </div>
    </article>
  ))}
</div>`}</code></pre>
      </div>

      <!-- Style 2: Featured + Grid (from Hub) -->
      <div class="mb-12 rounded-lg border-2 border-primary bg-primary/5 p-6 dark:bg-primary/10">
        <h3 class="mb-4 text-2xl font-semibold text-slate-900 dark:text-white">Style 2: Featured + Grid (From Hub Page)</h3>
        <p class="mb-4 text-slate-600 dark:text-slate-300">
          <strong>What it looks like:</strong> At the top, one large featured testimonial in a special box with a blue gradient background. The person's photo (large, circular) is on the left, testimonial text (larger font) and name/job are on the right. Below that, a regular 3-column grid of other testimonials using minimal card style (thin border, no shadow) with placeholder avatars (gray gradient circles instead of photos).
        </p>
        <pre class="mb-4 overflow-x-auto rounded bg-slate-900 p-4 text-sm text-slate-100"><code>{`---
import '~/styles/testimonials.css';
import { getFeaturedTestimonials, testimonials } from '~/data/testimonials';

const featured = getFeaturedTestimonials().slice(0, 1);
const others = testimonials.slice(0, 6);
---

{featured.length > 0 && (
  <div class="mb-8 rounded-2xl border border-primary/20 bg-gradient-to-br from-primary/10 to-primary/5 p-8 shadow-lg dark:border-primary/30 dark:from-primary/20 dark:to-primary/10">
    {featured.map((item) => (
      <div class="grid gap-6 md:grid-cols-[auto,1fr] md:items-center">
        <div class="testimonial-avatar testimonial-avatar-show testimonial-avatar-lg rounded-full overflow-hidden">
          <img src={item.image.src} alt={item.image.alt} class="h-full w-full object-cover" />
        </div>
        <div>
          {item.rating && (
            <div class="testimonial-rating testimonial-rating-show mb-3 flex items-center gap-1" data-rating={item.rating}>
              {[0, 1, 2, 3, 4].map((i) => (
                <svg
                  class={\`testimonial-star \${i < item.rating ? 'text-yellow-400 fill-current' : 'testimonial-star-empty'}\`}
                  viewBox="0 0 24 24"
                  aria-hidden="true"
                >
                  <path
                    stroke-linecap="round"
                    stroke-linejoin="round"
                    stroke-width="2"
                    d="M11.049 2.927c.3-.921 1.603-.921 1.902 0l1.519 4.674a1 1 0 00.95.69h4.915c.969 0 1.371 1.24.588 1.81l-3.976 2.888a1 1 0 00-.363 1.118l1.518 4.674c.3.922-.755 1.688-1.538 1.118l-3.976-2.888a1 1 0 00-1.176 0l-3.976 2.888c-.783.57-1.838-.197-1.538-1.118l1.518-4.674a1 1 0 00-.363-1.118l-3.976-2.888c-.784-.57-.38-1.81.588-1.81h4.914a1 1 0 00.951-.69l1.519-4.674z"
                  />
                </svg>
              ))}
            </div>
          )}
          <p class="testimonial-content text-lg text-slate-900 dark:text-white">{item.testimonial}</p>
          <div class="mt-4 testimonial-author">
            <p class="font-semibold text-slate-900 dark:text-white">{item.name}</p>
            <p class="text-sm text-slate-500 dark:text-slate-400">{item.job}</p>
          </div>
        </div>
      </div>
    ))}
  </div>
)}

<div class="testimonial-grid">
  {others.map((item) => (
    <article class="testimonial-card testimonial-card-minimal">
      <p class="testimonial-content text-slate-800 dark:text-slate-200">{item.testimonial}</p>
      <div class="mt-4 flex items-center gap-3">
        <div class="testimonial-avatar testimonial-avatar-placeholder testimonial-avatar-md rounded-full"></div>
        <div class="testimonial-author">
          <p class="font-semibold text-slate-900 dark:text-white">{item.name}</p>
          <p class="text-sm text-slate-500 dark:text-slate-400">{item.job}</p>
        </div>
      </div>
    </article>
  ))}
</div>`}</code></pre>
      </div>

      <!-- Style 3: Masonry (from Hub) -->
      <div class="mb-12 rounded-lg border-2 border-primary bg-primary/5 p-6 dark:bg-primary/10">
        <h3 class="mb-4 text-2xl font-semibold text-slate-900 dark:text-white">Style 3: Masonry Layout (From Hub Page)</h3>
        <p class="mb-4 text-slate-600 dark:text-slate-300">
          <strong>What it looks like:</strong> Testimonials arranged in columns like a brick wall. Short testimonials sit next to tall ones. No profile pictures - just the testimonial text, name, and job title. Cards are different heights based on text length, creating a dynamic, Pinterest-style layout.
        </p>
        <pre class="mb-4 overflow-x-auto rounded bg-slate-900 p-4 text-sm text-slate-100"><code>{`---
import '~/styles/testimonials.css';
import { testimonials } from '~/data/testimonials';

const myTestimonials = testimonials.slice(0, 9);
---

<div class="testimonial-masonry">
  {myTestimonials.map((item) => (
    <article class="testimonial-masonry-item testimonial-card">
      <p class="testimonial-content text-slate-800 dark:text-slate-200">{item.testimonial}</p>
      <div class="mt-4 flex items-center gap-3">
        <div class="testimonial-avatar testimonial-avatar-hide testimonial-avatar-md rounded-full overflow-hidden"></div>
        <div class="testimonial-author">
          <p class="font-semibold text-slate-900 dark:text-white">{item.name}</p>
          <p class="text-sm text-slate-500 dark:text-slate-400">{item.job}</p>
        </div>
      </div>
    </article>
  ))}
</div>`}</code></pre>
      </div>

      <!-- Style 4: Video Testimonials (from Hub) -->
      <div class="mb-12 rounded-lg border-2 border-primary bg-primary/5 p-6 dark:bg-primary/10">
        <h3 class="mb-4 text-2xl font-semibold text-slate-900 dark:text-white">Style 4: Video Testimonials (From Hub Page)</h3>
        <p class="mb-4 text-slate-600 dark:text-slate-300">
          <strong>What it looks like:</strong> A grid of video testimonial cards. Each card shows: a video thumbnail (either wide 16:9 or tall 9:16 format) with a play button overlay in the center, video duration and view count in the bottom-left corner, the testimonial text below the video, and then the person's profile picture, name, and job title.
        </p>
        <pre class="mb-4 overflow-x-auto rounded bg-slate-900 p-4 text-sm text-slate-100"><code>{`---
import '~/styles/testimonials.css';
import { getVideoTestimonials } from '~/data/testimonials';

const myTestimonials = getVideoTestimonials().slice(0, 3);
---

<div class="grid gap-6 lg:grid-cols-3">
  {myTestimonials.map((item) => (
    <article class="testimonial-card p-0 overflow-hidden">
      <div class={\`relative \${item.video?.type === 'vertical' ? 'aspect-[9/16]' : 'aspect-video'} bg-slate-900\`}>
        <img src={item.video?.thumbnail ?? item.image.src} alt={item.image.alt} class="h-full w-full object-cover" />
        <div class="absolute inset-0 flex items-center justify-center">
          <div class="rounded-full bg-white/90 px-4 py-2 text-sm font-semibold text-slate-900 shadow">▶ Play</div>
        </div>
        <div class="absolute bottom-3 left-3 flex items-center gap-2 rounded-full bg-slate-900/80 px-3 py-1 text-xs text-white backdrop-blur">
          {item.video?.duration && <span>{item.video.duration}</span>}
          {item.views && <span class="opacity-80">• {item.views} views</span>}
        </div>
      </div>
      <div class="p-5">
        <p class="testimonial-content text-slate-800 dark:text-slate-200">{item.testimonial}</p>
        <div class="mt-4 flex items-center gap-3">
          <div class="testimonial-avatar testimonial-avatar-show testimonial-avatar-md rounded-full overflow-hidden">
            <img src={item.image.src} alt={item.image.alt} class="h-full w-full object-cover" />
          </div>
          <div class="testimonial-author">
            <p class="font-semibold text-slate-900 dark:text-white">{item.name}</p>
            <p class="text-sm text-slate-500 dark:text-slate-400">{item.job}</p>
          </div>
        </div>
      </div>
    </article>
  ))}
</div>`}</code></pre>
      </div>

      <!-- Style 5: Long Form (from Hub) -->
      <div class="mb-12 rounded-lg border-2 border-primary bg-primary/5 p-6 dark:bg-primary/10">
        <h3 class="mb-4 text-2xl font-semibold text-slate-900 dark:text-white">Style 5: Long Form Testimonials (From Hub Page)</h3>
        <p class="mb-4 text-slate-600 dark:text-slate-300">
          <strong>What it looks like:</strong> Testimonials displayed one below the other (not in a grid). Each testimonial is in a minimal card (thin border, no shadow) with lots of padding for easy reading. Large profile pictures on the left, testimonial text (larger font) in the middle, and name/job below. Perfect for detailed, story-driven testimonials that are 300+ characters long.
        </p>
        <pre class="mb-4 overflow-x-auto rounded bg-slate-900 p-4 text-sm text-slate-100"><code>{`---
import '~/styles/testimonials.css';
import { getLongTestimonials } from '~/data/testimonials';

const myTestimonials = getLongTestimonials();
---

<div class="space-y-6">
  {myTestimonials.map((item) => (
    <article class="testimonial-card testimonial-card-minimal">
      <p class="testimonial-content text-lg text-slate-800 dark:text-slate-100">{item.testimonial}</p>
      <div class="mt-6 flex items-center gap-4">
        <div class="testimonial-avatar testimonial-avatar-show testimonial-avatar-lg rounded-full overflow-hidden">
          <img src={item.image.src} alt={item.image.alt} class="h-full w-full object-cover" />
        </div>
        <div class="testimonial-author">
          <p class="text-lg font-semibold text-slate-900 dark:text-white">{item.name}</p>
          <p class="text-sm text-slate-500 dark:text-slate-400">{item.job}</p>
        </div>
      </div>
    </article>
  ))}
</div>`}</code></pre>
      </div>
    </section>

    <!-- CUSTOMIZATION -->
    <section id="customization" class="mb-16 scroll-mt-20">
      <div class="mb-6">
        <h2 class="text-3xl font-bold text-slate-900 dark:text-white">Customization Options</h2>
        <p class="mt-2 text-slate-600 dark:text-slate-300">
          Easy ways to customize how your testimonials look.
        </p>
      </div>

      <!-- Avatar Options -->
      <div class="mb-8 rounded-lg border border-slate-200 bg-white p-6 dark:border-slate-800 dark:bg-slate-900">
        <h3 class="mb-4 text-xl font-semibold text-slate-900 dark:text-white">Avatar Display Options</h3>
        <div class="space-y-3">
          <div>
            <p class="mb-2 font-semibold text-slate-700 dark:text-slate-300">Show Profile Picture (Default)</p>
            <code class="block rounded bg-slate-100 p-2 text-sm dark:bg-slate-800">testimonial-avatar testimonial-avatar-show testimonial-avatar-md</code>
            <p class="mt-1 text-xs text-slate-500">Shows the person's actual photo in a circle</p>
          </div>
          <div>
            <p class="mb-2 font-semibold text-slate-700 dark:text-slate-300">Hide Avatar</p>
            <code class="block rounded bg-slate-100 p-2 text-sm dark:bg-slate-800">testimonial-avatar testimonial-avatar-hide</code>
            <p class="mt-1 text-xs text-slate-500">No photo shown - just name and job title</p>
          </div>
          <div>
            <p class="mb-2 font-semibold text-slate-700 dark:text-slate-300">Show Placeholder (Gradient Box)</p>
            <code class="block rounded bg-slate-100 p-2 text-sm dark:bg-slate-800">testimonial-avatar testimonial-avatar-placeholder testimonial-avatar-md</code>
            <p class="mt-1 text-xs text-slate-500">Shows a gray gradient circle instead of a photo</p>
          </div>
        </div>
        <p class="mt-4 text-sm text-slate-600 dark:text-slate-400">
          <strong>Avatar Sizes:</strong> sm (40px), md (48px), lg (64px), xl (80px), 2xl (128px) - replace "md" with your preferred size
        </p>
      </div>

      <!-- Card Styles -->
      <div class="mb-8 rounded-lg border border-slate-200 bg-white p-6 dark:border-slate-800 dark:bg-slate-900">
        <h3 class="mb-4 text-xl font-semibold text-slate-900 dark:text-white">Card Style Options</h3>
        <div class="space-y-3">
          <div>
            <p class="mb-2 font-semibold text-slate-700 dark:text-slate-300">Standard Card (with shadow)</p>
            <code class="block rounded bg-slate-100 p-2 text-sm dark:bg-slate-800">testimonial-card</code>
            <p class="mt-1 text-xs text-slate-500">White card with subtle shadow underneath</p>
          </div>
          <div>
            <p class="mb-2 font-semibold text-slate-700 dark:text-slate-300">Minimal Card (border only)</p>
            <code class="block rounded bg-slate-100 p-2 text-sm dark:bg-slate-800">testimonial-card testimonial-card-minimal</code>
            <p class="mt-1 text-xs text-slate-500">White card with thin border, no shadow</p>
          </div>
          <div>
            <p class="mb-2 font-semibold text-slate-700 dark:text-slate-300">Featured Card (gradient background)</p>
            <code class="block rounded bg-slate-100 p-2 text-sm dark:bg-slate-800">testimonial-card testimonial-card-featured</code>
            <p class="mt-1 text-xs text-slate-500">Card with blue gradient background to stand out</p>
          </div>
        </div>
      </div>

      <!-- Layout Options -->
      <div class="mb-8 rounded-lg border border-slate-200 bg-white p-6 dark:border-slate-800 dark:bg-slate-900">
        <h3 class="mb-4 text-xl font-semibold text-slate-900 dark:text-white">Layout Options</h3>
        <div class="space-y-3">
          <div>
            <p class="mb-2 font-semibold text-slate-700 dark:text-slate-300">3-Column Grid (1/2/3 columns responsive)</p>
            <code class="block rounded bg-slate-100 p-2 text-sm dark:bg-slate-800">testimonial-grid</code>
            <p class="mt-1 text-xs text-slate-500">1 column on phone, 2 on tablet, 3 on desktop</p>
          </div>
          <div>
            <p class="mb-2 font-semibold text-slate-700 dark:text-slate-300">2-Column Grid (1/2 columns responsive)</p>
            <code class="block rounded bg-slate-100 p-2 text-sm dark:bg-slate-800">testimonial-grid-2</code>
            <p class="mt-1 text-xs text-slate-500">1 column on phone, 2 on larger screens</p>
          </div>
          <div>
            <p class="mb-2 font-semibold text-slate-700 dark:text-slate-300">Masonry (staggered columns)</p>
            <code class="block rounded bg-slate-100 p-2 text-sm dark:bg-slate-800">testimonial-masonry</code>
            <p class="mt-1 text-xs text-slate-500">Pinterest-style staggered layout</p>
          </div>
        </div>
      </div>
    </section>

    <!-- QUICK REFERENCE -->
    <section class="mb-16 rounded-2xl border-2 border-primary bg-primary/5 p-8 dark:bg-primary/10">
      <h2 class="mb-6 text-3xl font-bold text-slate-900 dark:text-white">Quick Reference Cheat Sheet</h2>
      <div class="grid gap-6 md:grid-cols-2">
        <div>
          <h3 class="mb-3 font-semibold text-slate-900 dark:text-white">Selecting Testimonials</h3>
          <ul class="space-y-2 text-sm text-slate-600 dark:text-slate-300">
            <li>• <code class="rounded bg-slate-200 px-1 dark:bg-slate-700">testimonials.slice(0, 3)</code> - First 3</li>
            <li>• <code class="rounded bg-slate-200 px-1 dark:bg-slate-700">getFeaturedTestimonials()</code> - Featured only</li>
            <li>• <code class="rounded bg-slate-200 px-1 dark:bg-slate-700">getTestimonialsByCategory('coaching')</code> - By category</li>
            <li>• <code class="rounded bg-slate-200 px-1 dark:bg-slate-700">getRatedTestimonials()</code> - With ratings</li>
            <li>• <code class="rounded bg-slate-200 px-1 dark:bg-slate-700">getVideoTestimonials()</code> - Video only</li>
          </ul>
        </div>
        <div>
          <h3 class="mb-3 font-semibold text-slate-900 dark:text-white">Layout Classes</h3>
          <ul class="space-y-2 text-sm text-slate-600 dark:text-slate-300">
            <li>• <code class="rounded bg-slate-200 px-1 dark:bg-slate-700">testimonial-grid</code> - 3 columns</li>
            <li>• <code class="rounded bg-slate-200 px-1 dark:bg-slate-700">testimonial-grid-2</code> - 2 columns</li>
            <li>• <code class="rounded bg-slate-200 px-1 dark:bg-slate-700">testimonial-masonry</code> - Staggered</li>
            <li>• <code class="rounded bg-slate-200 px-1 dark:bg-slate-700">testimonial-card</code> - Standard</li>
            <li>• <code class="rounded bg-slate-200 px-1 dark:bg-slate-700">testimonial-card-minimal</code> - Minimal</li>
          </ul>
        </div>
      </div>
    </section>

    <!-- NEED HELP -->
    <section class="rounded-2xl border border-slate-200 bg-slate-50 p-8 dark:border-slate-800 dark:bg-slate-900/50">
      <h2 class="mb-4 text-2xl font-bold text-slate-900 dark:text-white">Need More Help?</h2>
      <p class="mb-4 text-slate-600 dark:text-slate-300">
        Check out the <a href="/testimonials-hub" class="text-primary hover:underline">Testimonials Hub</a> page to see all styles in action with live examples.
      </p>
      <p class="text-slate-600 dark:text-slate-300">
        All testimonial data is stored in <code class="rounded bg-slate-200 px-1 dark:bg-slate-700">src/data/testimonials.ts</code> and all styles are in <code class="rounded bg-slate-200 px-1 dark:bg-slate-700">src/styles/testimonials.css</code>
      </p>
    </section>

  </div>
</Layout>
